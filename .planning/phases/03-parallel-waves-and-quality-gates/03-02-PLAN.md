---
phase: 03-parallel-waves-and-quality-gates
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/judges/sub-judge-panel.ts
  - src/judges/tsc-judge.ts
  - src/judges/vitest-judge.ts
  - src/judges/touch-map-judge.ts
  - tests/unit/sub-judge-panel.test.ts
autonomous: true
requirements: [REVW-01, REVW-01a, REVW-02]

must_haves:
  truths:
    - "Sub-Judges run in parallel after every wave with mechanical checks"
    - "The v1 Sub-Judge set includes tsc check, touch-map violation detector, and vitest run (if tests exist)"
    - "Sub-Judge failure is reported with details but does not halt the current wave's completion"
    - "All Sub-Judge results are collected into a SubJudgeReport with allPassed boolean"
  artifacts:
    - path: "src/judges/sub-judge-panel.ts"
      provides: "runSubJudges() orchestrator that runs all checks in parallel"
      exports: ["runSubJudges"]
    - path: "src/judges/tsc-judge.ts"
      provides: "runTscCheck() spawns tsc --noEmit via child_process"
      exports: ["runTscCheck"]
    - path: "src/judges/vitest-judge.ts"
      provides: "runVitestCheck() spawns vitest run if test files exist"
      exports: ["runVitestCheck"]
    - path: "src/judges/touch-map-judge.ts"
      provides: "runTouchMapCheck() validates merged diff against task touch maps"
      exports: ["runTouchMapCheck"]
    - path: "tests/unit/sub-judge-panel.test.ts"
      provides: "Tests for panel orchestration and individual judges"
  key_links:
    - from: "src/judges/sub-judge-panel.ts"
      to: "src/judges/tsc-judge.ts"
      via: "import runTscCheck"
      pattern: "runTscCheck\\(projectDir\\)"
    - from: "src/judges/sub-judge-panel.ts"
      to: "src/judges/vitest-judge.ts"
      via: "import runVitestCheck"
      pattern: "runVitestCheck\\(projectDir\\)"
    - from: "src/judges/sub-judge-panel.ts"
      to: "src/judges/touch-map-judge.ts"
      via: "import runTouchMapCheck"
      pattern: "runTouchMapCheck"
    - from: "src/judges/sub-judge-panel.ts"
      to: "src/schemas/reports.ts"
      via: "returns SubJudgeReport type"
      pattern: "SubJudgeReport"
---

<objective>
Build the Sub-Judge panel: three mechanical quality checks (tsc, vitest, touch-map) that run in parallel after each wave and produce a SubJudgeReport gating progression.

Purpose: Implement REVW-01, REVW-01a, REVW-02 -- mechanical quality gates that catch errors between waves before they cascade. Sub-Judges are deterministic (no AI), using existing tools via child_process.
Output: `src/judges/` directory with panel orchestrator and three individual judges, plus comprehensive tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-parallel-waves-and-quality-gates/03-RESEARCH.md

@src/schemas/reports.ts
@src/schemas/plan.ts
@src/git/worktree-manager.ts
@package.json

<interfaces>
<!-- Key types the executor needs. -->

From src/schemas/reports.ts:
```typescript
export const SubJudgeCheckSchema = z.object({
  name: z.string(),
  passed: z.boolean(),
  message: z.string().optional(),
  details: z.string().optional(),
});

export const SubJudgeReportSchema = z.object({
  waveNumber: z.number().int(),
  checks: z.array(SubJudgeCheckSchema),
  allPassed: z.boolean(),
  timestamp: z.string().datetime(),
});

export type SubJudgeCheck = z.infer<typeof SubJudgeCheckSchema>;
export type SubJudgeReport = z.infer<typeof SubJudgeReportSchema>;
```

From src/schemas/plan.ts:
```typescript
export type Task = {
  id: string;
  description: string;
  writes: string[];
  reads: string[];
  dependsOn: string[];
  acceptanceCriteria: string[];
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build three Sub-Judge checkers (tsc, vitest, touch-map)</name>
  <files>
    src/judges/tsc-judge.ts,
    src/judges/vitest-judge.ts,
    src/judges/touch-map-judge.ts,
    tests/unit/sub-judge-panel.test.ts
  </files>
  <read_first>
    src/schemas/reports.ts,
    src/schemas/plan.ts,
    src/git/worktree-manager.ts,
    .planning/phases/03-parallel-waves-and-quality-gates/03-RESEARCH.md
  </read_first>
  <behavior>
    tsc-judge tests:
    - When tsc --noEmit succeeds (exit 0), returns SubJudgeCheck with name="tsc", passed=true
    - When tsc --noEmit fails (exit 1), returns passed=false with message and details containing stderr/stdout
    - When tsconfig.json does not exist in projectDir, returns passed=true with message "skipped: no tsconfig.json"
    - Timeout of 60 seconds is set on the child process

    vitest-judge tests:
    - When vitest run succeeds (exit 0), returns SubJudgeCheck with name="vitest", passed=true
    - When vitest run fails (exit 1), returns passed=false with details
    - When no test files exist (no **/*.test.* or **/*.spec.*), returns passed=true with message "skipped: no test files found"
    - Uses `npx vitest run` (not `vitest` alone -- avoids watch mode)
    - Timeout of 120 seconds, stdio: 'pipe' to detach from TTY

    touch-map-judge tests:
    - Given a baselineSha and current HEAD, diffs files changed and checks against allowed writes from tasks
    - Files within allowed writes = passed=true
    - Files outside allowed writes = passed=false with violation list in details
    - When no files changed, returns passed=true
  </behavior>
  <action>
    1. Create `src/judges/tsc-judge.ts`:
       - Export `async function runTscCheck(projectDir: string): Promise<SubJudgeCheck>`
       - First check if `tsconfig.json` exists in projectDir using `stat()`. If not, return passed=true with message "skipped: no tsconfig.json"
       - Use `promisify(execFile)('npx', ['tsc', '--noEmit'], { cwd: projectDir, timeout: 60_000 })`
       - On success: `{ name: 'tsc', passed: true }`
       - On failure: `{ name: 'tsc', passed: false, message: 'TypeScript compilation failed', details: stdout + stderr }`
       - Import `SubJudgeCheck` type from `'../schemas/reports.js'`

    2. Create `src/judges/vitest-judge.ts`:
       - Export `async function runVitestCheck(projectDir: string): Promise<SubJudgeCheck>`
       - First check if any test files exist using glob pattern `**/*.test.{ts,tsx,js,jsx}` or `**/*.spec.{ts,tsx,js,jsx}` via a recursive readdir or execFile with `find`. Use `execFile('find', [projectDir, '-name', '*.test.ts', '-o', '-name', '*.test.tsx', '-o', '-name', '*.spec.ts', '-o', '-name', '*.test.js'], { timeout: 5000 })` as a quick check. If no test files, return passed=true with message "skipped: no test files found"
       - Run `npx vitest run` with `{ cwd: projectDir, timeout: 120_000, stdio: 'pipe' }` -- CRITICAL: always `vitest run`, never bare `vitest`
       - On success: `{ name: 'vitest', passed: true }`
       - On failure: `{ name: 'vitest', passed: false, message: 'Test suite failed', details: stdout + stderr }`

    3. Create `src/judges/touch-map-judge.ts`:
       - Export `async function runTouchMapCheck(projectDir: string, baselineSha: string, tasks: Task[]): Promise<SubJudgeCheck>`
       - The `baselineSha` is the git HEAD SHA captured before wave merges started (caller provides this)
       - Use `simpleGit(projectDir).diff(['--name-only', baselineSha, 'HEAD'])` to get files changed during wave
       - Build `allowedWrites` set from `tasks.flatMap(t => t.writes)`
       - Check each changed file against allowedWrites
       - No violations: `{ name: 'touch-map', passed: true }`
       - Violations found: `{ name: 'touch-map', passed: false, message: 'N file(s) modified outside declared writes[]', details: violations.join('\n') }`

    4. Write tests FIRST (TDD red), then implement to make them green.
       - For tsc and vitest judges, mock `execFile` using vitest's `vi.mock('node:child_process', ...)`
       - For touch-map judge, create a real temp git repo (like existing worktree tests) to verify diff behavior
       - For "file not found" checks, use temp directories without the relevant config files
  </action>
  <verify>
    <automated>cd /Users/address0/Documents/Repos/anvil && npx vitest run tests/unit/sub-judge-panel.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - `runTscCheck` exported from `src/judges/tsc-judge.ts`
    - `runVitestCheck` exported from `src/judges/vitest-judge.ts`
    - `runTouchMapCheck` exported from `src/judges/touch-map-judge.ts`
    - Each function returns `SubJudgeCheck` matching the schema from `src/schemas/reports.ts`
    - tsc judge skips when no tsconfig.json exists
    - vitest judge skips when no test files exist
    - vitest judge uses `vitest run` (not bare `vitest`) with timeout and piped stdio
    - touch-map judge accepts baselineSha parameter (not hardcoded HEAD~N)
    - All unit tests pass (9+ test cases covering success/failure/skip for each judge)
  </acceptance_criteria>
  <done>Three Sub-Judge checkers are built, each returning SubJudgeCheck, with skip logic for missing configs, all tests green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build Sub-Judge panel orchestrator</name>
  <files>
    src/judges/sub-judge-panel.ts,
    tests/unit/sub-judge-panel.test.ts
  </files>
  <read_first>
    src/judges/tsc-judge.ts,
    src/judges/vitest-judge.ts,
    src/judges/touch-map-judge.ts,
    src/schemas/reports.ts
  </read_first>
  <behavior>
    sub-judge-panel orchestrator tests (add to existing test file):
    - runSubJudges calls all three judges and returns SubJudgeReport
    - When all judges pass, allPassed=true
    - When one judge fails, allPassed=false but all checks are present in the report
    - Report has correct waveNumber and ISO timestamp
    - Judges run in parallel (Promise.all), not sequentially
    - Report is saved to `.anvil/reports/wave-{N}-judges.json` in the project directory
  </behavior>
  <action>
    1. Create `src/judges/sub-judge-panel.ts`:
       - Export `async function runSubJudges(projectDir: string, waveNumber: number, tasks: Task[], baselineSha: string): Promise<SubJudgeReport>`
       - Import all three judges
       - Run all three in parallel via `Promise.all([runTscCheck(projectDir), runVitestCheck(projectDir), runTouchMapCheck(projectDir, baselineSha, tasks)])`
       - Construct `SubJudgeReport`: `{ waveNumber, checks, allPassed: checks.every(c => c.passed), timestamp: new Date().toISOString() }`
       - Save report to `join(projectDir, '.anvil', 'reports', 'wave-{waveNumber}-judges.json')` using `writeFile` with `mkdir({ recursive: true })`
       - Return the report

    2. Add orchestrator tests to `tests/unit/sub-judge-panel.test.ts` (append to the file created in Task 1):
       - Mock the three judge functions to return controlled results
       - Verify allPassed logic
       - Verify report structure matches SubJudgeReportSchema (use zod parse)
       - Verify file is written to correct path
  </action>
  <verify>
    <automated>cd /Users/address0/Documents/Repos/anvil && npx vitest run tests/unit/sub-judge-panel.test.ts -x && npx vitest run -x</automated>
  </verify>
  <acceptance_criteria>
    - `runSubJudges` is exported from `src/judges/sub-judge-panel.ts`
    - Function signature includes `baselineSha: string` parameter
    - All three judges are called via `Promise.all` (parallel execution)
    - `allPassed` is computed as `checks.every(c => c.passed)`
    - Report validates against `SubJudgeReportSchema` from `src/schemas/reports.ts`
    - Report saved to `.anvil/reports/wave-{N}-judges.json`
    - All tests pass (orchestrator + individual judges)
    - Full test suite passes: `npx vitest run`
  </acceptance_criteria>
  <done>Sub-Judge panel orchestrates three mechanical checks in parallel, produces SubJudgeReport, saves to disk, all tests green</done>
</task>

</tasks>

<verification>
1. `npx vitest run` — all tests pass (existing + new)
2. `npx tsc --noEmit` — no type errors
3. `grep -r "runSubJudges" src/` confirms export
4. `grep -r "runTscCheck\|runVitestCheck\|runTouchMapCheck" src/judges/` confirms all judges
5. `ls src/judges/` shows 4 files: sub-judge-panel.ts, tsc-judge.ts, vitest-judge.ts, touch-map-judge.ts
</verification>

<success_criteria>
- Three mechanical Sub-Judges: tsc, vitest, touch-map
- Each returns SubJudgeCheck with pass/fail/skip logic
- Panel runs all three in parallel via Promise.all
- Report matches SubJudgeReportSchema with allPassed boolean
- Report persisted to .anvil/reports/
- 15+ test cases covering each judge and the orchestrator
</success_criteria>

<output>
After completion, create `.planning/phases/03-parallel-waves-and-quality-gates/03-02-SUMMARY.md`
</output>
