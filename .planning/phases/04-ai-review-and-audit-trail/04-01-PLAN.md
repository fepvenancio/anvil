---
phase: 04-ai-review-and-audit-trail
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/schemas/reports.ts
  - src/cost/tracker.ts
  - src/cost/pricing.ts
  - tests/unit/cost-tracker.test.ts
autonomous: true
requirements:
  - COST-01
  - COST-02

must_haves:
  truths:
    - "CostTracker accumulates token usage from Anthropic SDK response.usage objects"
    - "CostTracker computes per-wave and per-session cost totals using model pricing"
    - "CostEntry schema includes optional waveNumber field for per-wave grouping"
  artifacts:
    - path: "src/cost/tracker.ts"
      provides: "CostTracker class with record, recordFromResponse, toCostReport, getWaveCost, getSessionCost"
      exports: ["CostTracker"]
    - path: "src/cost/pricing.ts"
      provides: "Model pricing lookup table and calculateCost function"
      exports: ["MODEL_PRICING", "calculateCost"]
    - path: "src/schemas/reports.ts"
      provides: "CostEntrySchema with optional waveNumber field"
      contains: "waveNumber"
    - path: "tests/unit/cost-tracker.test.ts"
      provides: "Unit tests for CostTracker and pricing"
  key_links:
    - from: "src/cost/tracker.ts"
      to: "src/cost/pricing.ts"
      via: "calculateCost import"
      pattern: "import.*calculateCost.*from.*pricing"
    - from: "src/cost/tracker.ts"
      to: "src/schemas/reports.ts"
      via: "CostReport type"
      pattern: "import.*CostReport.*from.*schemas/reports"
---

<objective>
Build the cost tracking infrastructure: a CostTracker accumulator class, model pricing lookup table, and CostEntry schema update. This is the foundation that all other Phase 4 components (High Court, Librarian, CLI wiring) will use to record their token usage.

Purpose: COST-01 and COST-02 require token tracking per agent call and cost calculation per wave/session. This plan creates those primitives.
Output: CostTracker class, pricing module, updated CostEntry schema, passing unit tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ai-review-and-audit-trail/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/schemas/reports.ts:
```typescript
export const CostEntrySchema = z.object({
  agent: z.string(),
  inputTokens: z.number().int(),
  outputTokens: z.number().int(),
  cacheReadTokens: z.number().int().default(0),
  cacheWriteTokens: z.number().int().default(0),
  costUsd: z.number(),
});

export const CostReportSchema = z.object({
  sessionId: z.string(),
  entries: z.array(CostEntrySchema),
  totalCostUsd: z.number(),
  timestamp: z.string().datetime(),
});

export type CostEntry = z.infer<typeof CostEntrySchema>;
export type CostReport = z.infer<typeof CostReportSchema>;
```

From Anthropic SDK response.usage:
```typescript
// response.usage fields available on every messages.create() / messages.parse():
interface Usage {
  input_tokens: number;
  output_tokens: number;
  cache_creation_input_tokens?: number;
  cache_read_input_tokens?: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CostEntry schema update + pricing module + CostTracker class</name>
  <files>src/schemas/reports.ts, src/cost/pricing.ts, src/cost/tracker.ts, tests/unit/cost-tracker.test.ts</files>
  <read_first>src/schemas/reports.ts, src/stations/planner.ts, src/workers/worker.ts, .planning/phases/04-ai-review-and-audit-trail/04-RESEARCH.md</read_first>
  <behavior>
    - Test 1: CostTracker.record() stores a TokenUsage entry and entries array grows
    - Test 2: CostTracker.recordFromResponse() extracts usage from Anthropic-style response object ({usage: {input_tokens, output_tokens, cache_creation_input_tokens, cache_read_input_tokens}})
    - Test 3: calculateCost() returns correct USD for known token counts with claude-sonnet-4-20250514 pricing (3/15/3.75/0.30 per MTok)
    - Test 4: calculateCost() falls back to sonnet pricing for unknown model strings
    - Test 5: CostTracker.toCostReport(sessionId) produces a valid CostReport with correct totalCostUsd sum
    - Test 6: CostTracker.getWaveCost(waveNumber) returns sum of costUsd for entries matching that wave
    - Test 7: CostTracker.getSessionCost() returns total across all entries
    - Test 8: CostEntrySchema validates objects with optional waveNumber field
    - Test 9: CostEntrySchema validates objects without waveNumber (backward compat)
  </behavior>
  <action>
    **Step 1: Update CostEntrySchema** in `src/schemas/reports.ts`:
    - Add `waveNumber: z.number().int().optional()` to CostEntrySchema (after cacheWriteTokens, before costUsd)
    - Do NOT change any other schema in the file

    **Step 2: Create `src/cost/pricing.ts`:**
    - Export `ModelPricing` interface: `{ inputPerMTok: number, outputPerMTok: number, cacheWritePerMTok: number, cacheReadPerMTok: number }`
    - Export `MODEL_PRICING: Record<string, ModelPricing>` with entries for:
      - `claude-sonnet-4-20250514`: 3 / 15 / 3.75 / 0.30
      - `claude-haiku-4-5-20250514`: 1 / 5 / 1.25 / 0.10
    - Export `calculateCost(usage: { inputTokens: number, outputTokens: number, cacheReadTokens: number, cacheWriteTokens: number, model: string }): number`
    - Fallback to sonnet pricing if model not found in lookup
    - Calculation: `(inputTokens / 1_000_000) * inputPerMTok + (outputTokens / 1_000_000) * outputPerMTok + (cacheWriteTokens / 1_000_000) * cacheWritePerMTok + (cacheReadTokens / 1_000_000) * cacheReadPerMTok`

    **Step 3: Create `src/cost/tracker.ts`:**
    - Export `TokenUsage` interface: `{ agent: string, inputTokens: number, outputTokens: number, cacheReadTokens: number, cacheWriteTokens: number, model: string, waveNumber?: number }`
    - Export `CostTracker` class with:
      - `private entries: TokenUsage[]` initialized to `[]`
      - `record(usage: TokenUsage): void` — pushes to entries
      - `recordFromResponse(response: { usage: { input_tokens: number, output_tokens: number, cache_creation_input_tokens?: number, cache_read_input_tokens?: number } }, agent: string, model: string, waveNumber?: number): void` — extracts SDK fields and calls record()
      - `toCostReport(sessionId: string): CostReport` — maps entries through calculateCost, sums totalCostUsd, returns CostReport with ISO timestamp
      - `getWaveCost(waveNumber: number): number` — filters entries by waveNumber, sums calculateCost for each
      - `getSessionCost(): number` — sums calculateCost for all entries
    - Import `calculateCost` from `./pricing.js`
    - Import `CostReport` type from `../schemas/reports.js`

    **Step 4: Write tests** in `tests/unit/cost-tracker.test.ts`:
    - Import CostTracker, calculateCost, MODEL_PRICING
    - All 9 behavior tests listed above
    - Use concrete numbers: 1000 input tokens at sonnet pricing = (1000/1_000_000)*3 = 0.003 USD
  </action>
  <verify>
    <automated>npx vitest run tests/unit/cost-tracker.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - CostEntrySchema in reports.ts accepts `{ agent: "planner", inputTokens: 100, outputTokens: 50, cacheReadTokens: 0, cacheWriteTokens: 0, costUsd: 0.001, waveNumber: 1 }` and also accepts objects without waveNumber
    - `calculateCost({ inputTokens: 1_000_000, outputTokens: 1_000_000, cacheReadTokens: 0, cacheWriteTokens: 0, model: 'claude-sonnet-4-20250514' })` returns 18 (3+15)
    - CostTracker.recordFromResponse() correctly extracts cache_creation_input_tokens and cache_read_input_tokens from SDK response
    - All 9 tests pass: `npx vitest run tests/unit/cost-tracker.test.ts -x`
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>CostTracker class accumulates token usage per agent call and computes per-wave and per-session costs. CostEntry schema supports optional waveNumber. All unit tests green.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/unit/cost-tracker.test.ts -x` — all tests pass
- `npx tsc --noEmit` — no type errors
- `npx vitest run` — full suite still green (no regressions from schema change)
</verification>

<success_criteria>
- CostTracker records token usage from Anthropic SDK responses (COST-01)
- Cost is calculated per wave and per session using model pricing table (COST-02)
- CostEntry schema includes optional waveNumber for per-wave grouping
- All tests pass, no type errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-ai-review-and-audit-trail/04-01-SUMMARY.md`
</output>
